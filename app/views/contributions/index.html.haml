.items-index
  .intro.row
    %h4 Help us enrich the WWQI archives by contributing information to the project.

    %h3 There are numerous ways you can contribute.

    %ul.thread-type.btn-group
      %li{class: "btn #{'active' if params[:type] == "transcription"} pull-left"}
        = link_to 'Transcribe a document', contributions_path(type: 'transcription')
      %li{class: "btn #{'active' if params[:type] == 'translation'} pull-left"}
        = link_to 'Translate a document', contributions_path(type: 'translation')
      %li{class: "btn #{'active' if params[:type] == 'biography'} pull-left"}
        = link_to 'Write a biography', contributions_path(type: 'biography')
      %li.btn.pull-left
        = link_to 'Submit a correction', new_correction_path
  #contributions-content
    #contribution-list
      %h4= "Items in need of #{params[:type]}".titleize
      %p.feed-intro= t('contributions.transcription.intro') 
      %p
        - case params[:type]
        - when 'transcription'
          = "Asking transcription contribution for selected by admin"
        - when 'translation'
          = "Asking translation contribution for selected by admin"
        - when 'biography'
          = "Asking biography contribution for selected by admin"
      %ul.items
        - unless @items.empty?
          - @items.each do |item|
            %li
              .item-image.pull-left
                = image_tag item.thumbnail ||= 'flag.png'
              .itme-content.pull-left
                .item-title
                  = item.name
              .transcribe.btn.pull-left
                = link_to params[:type].titleize, new_contribution_path(type: params[:type], item: item)
        -else
          %li
          %p= "No items in need of #{params[:type]}"
        %li
          .item-image.pull-left
          .item-content.pull-left
            .item-title Publication permit for Zaban-i zanan, 4 March 1944
          .transcribe.btn.pull-right
            = link_to 'Transcribe', new_contribution_path(type: params[:type])
        %li
          .item-image.pull-left
          .item-content.pull-left
            .item-title Lists of jewelry and kitchen items
          .transcribe.btn.pull-right
            = link_to 'Transcribe'
    .transcription-subnav
      %ul
        %li.pull-left
          %a.show-more{href: '#'}
            .icon-plus-circle#contributions-icon
            %span Show more documents
    #contribution-feed
      %h4 Recently contributed transcriptions
      %p.feed-intro Here are some recent transcriptions contributed by the WWQI users:
      %ul.items
        %li
          .item-image.pull-left
          .item-content.pull-left
            .item-header
              Maryam contributed a transcription for
            .item-title
              School diplomas of Houri Mostofi Moghadam
            .item-comments
              %p 2 comments
            %p.bullet &bull;
            .item-created
              %p 2 weeks ago
        %li
          .item-image.pull-left
          .item-content.pull-left
            .item-header
              Maryam contributed a transcription for
            .item-title
              Kanun-i Banuvan Cultural Services Cooperative, Articles of Association, 1964
            .item-comments
              %p 5 comments
            %p.bullet &bull;
            .item-created
              %p Last month
